// @flow

import { StyleSheet } from 'react-native'
import { Colors, FontSizes } from '../../Styles'

export default StyleSheet.create({
  syncText: {
    color: Colors.base.mutedTextInverse,
    textAlign: 'right',
    lineHeight: FontSizes.sm * 1.2,
  },
})
